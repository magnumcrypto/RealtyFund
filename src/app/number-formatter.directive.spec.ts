import { NumberFormatterDirective } from './number-formatter.directive';

describe('NumberFormatterDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberFormatterDirective();
    expect(directive).toBeTruthy();
  });
});
